const cars = ["Saab", "Volvo", "BMW"]; // Creating an array
const mixed=["Kushal", "Ram", [1,2]];

cars[1]; // Returns Volvo

cars[0]="Tata"; // Changing Value

//Properties of arrays

cars.length //Returns no. of elements in an array 3
cars.sort() // Sorts the array

// The Difference Between Arrays and Objects
// In JavaScript, arrays use numbered indexes.  

// In JavaScript, objects use named indexes.

Array.isArray(cars); // Returns True to check array  
